# Quy tắc dự án (Project Rules)

- Chỉ thực hiện đẩy mã nguồn lên GitHub (`git push`) khi người dùng yêu cầu rõ ràng bằng tin nhắn có nội dung như "đẩy dự án lên".
- Tất cả các chỉnh sửa mã nguồn khác sẽ chỉ được lưu cục bộ (local) trong thư mục làm việc của máy tính mà không tự động đẩy lên mạng.
